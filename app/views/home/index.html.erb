<div>
	<h1>Welcome to the Wilfley Engineering Database</h1>
	<% if notice %>
	  <p id="notice"><%= notice%></p>
	<% end %>
	<% if alert %>
	  <p id="notice"><%= alert%></p>
	<% end %>
	<p>Please use the options below to search, or use the menu above to choose other options.</p>
	<% if user_in_sales? || user_in_accounting? %>
	<div class="row">
		<div id="search" class="col-lg-6">
		  <%= render 'sir_search' %>
		</div>
		<div id="search" class="col-lg-6">
		  <%= render 'drawing_search' %>
		</div>
	</div>

	<% else %>
	
	<div class="row">
		<div id="search" class="span2">
		  <%= render 'drawing_search' %>
		</div>
		<div id="search" class="span2">
		  <%= render 'ecn_search' %>
		</div>
		<div id="search" class="span2">
		  <%= render 'sir_search' %>
		</div>
	</div>
	
	<% end %>
</div>
