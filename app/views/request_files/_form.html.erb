<%= form_for(@request_file) do |f| %>
  <% if @request_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_file.errors.count, "error") %> prohibited this request_file from being saved:</h2>

      <ul>
      <% @request_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_id %><br />
    <%= f.number_field :request_id %>
  </div>
  <div class="field">
    <%= f.label :file %><br />
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
